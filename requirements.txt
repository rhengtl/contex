# ConTeX runtime dependencies.
#
# Versions were last audited with pip-audit. The web-facing stack - Flask,
# Werkzeug, gunicorn, requests, urllib3, pillow, lxml - is kept current,
# because every one of them handles something an anonymous visitor supplies:
# the request itself, the uploaded image, the .docx, or the call that carries
# a password to Firebase.
#
# The local-OCR stack (torch, transformers, optimum, onnx, onnxruntime,
# datasets and what datasets drags in) is deliberately pinned old. See
# DEPLOYMENT.md, "Dependencies", for the exposure analysis and what moving it
# would involve - it is a migration, not a bump.

aiohappyeyeballs==2.6.1
aiohttp==3.14.3
aiosignal==1.4.0
attrs==25.3.0
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.4.2
click==8.3.3
colorama==0.4.6
coloredlogs==15.0.1
datasets==3.6.0
Deprecated==1.2.18
dill==0.3.8
pytesseract
firebase-admin==6.5.0
filelock==3.20.3
Flask==3.1.3
flatbuffers==25.2.10
frozenlist==1.6.0
python-dotenv==1.2.2
fsspec==2025.3.0
huggingface-hub==0.32.0
humanfriendly==10.0
idna==3.19
itsdangerous==2.2.0
Jinja2==3.1.6
lxml==6.1.0
MarkupSafe==3.0.2
mpmath==1.3.0
multidict==6.4.4
multiprocess==0.70.16
networkx==3.4.2
numpy==2.2.6
onnx==1.22.0
ml_dtypes==0.6.0
onnxruntime==1.22.0
optimum==1.17.1
packaging==25.0
pandas==2.2.3
pikepdf==9.7.0
pillow==12.3.0
propcache==0.3.1
protobuf==6.33.5
pyarrow==23.0.1
pyreadline3==3.5.4
python-dateutil==2.9.0.post0
pytz==2025.2
PyYAML==6.0.2
regex==2024.11.6
requests==2.33.0
safetensors==0.5.3
sentencepiece==0.2.1
setuptools==83.0.0
six==1.17.0
sympy==1.14.0
tokenizers==0.15.2
torch==2.7.0
tqdm==4.67.1
transformers==4.37.0
typing_extensions==4.16.0
tzdata==2025.2
urllib3==2.7.0
Werkzeug==3.1.6
wrapt==1.17.2
xxhash==3.5.0
yarl==1.20.0
gunicorn==23.0.0
pdf2image==1.17.0
# Word (.docx) input: read structurally, never OCR'd
python-docx==1.2.0
# AI OCR -> LaTeX: default provider (Gemini) and the opt-in Claude backend
google-genai>=2.0.0
anthropic>=1.0.0
